<Window x:Class="WpfApp.Views.PedidoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastro de Pedidos" Height="400" Width="500">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Cadastro de Pedido" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Pessoa:"/>
            <ComboBox ItemsSource="{Binding Pessoas}" 
                      SelectedItem="{Binding PessoaSelecionada}" 
                      DisplayMemberPath="Nome" Margin="0,2"/>

            <TextBlock Text="Produto:"/>
            <ComboBox ItemsSource="{Binding Produtos}" 
                      SelectedItem="{Binding ProdutoSelecionado}" 
                      DisplayMemberPath="Nome" Margin="0,2"/>

            <TextBlock Text="Quantidade:"/>
            <TextBox Text="{Binding Quantidade}" Margin="0,2"/>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="Adicionar" Command="{Binding AdicionarProdutoCommand}" Width="100" Margin="0,0,5,0"/>
                <Button Content="Finalizar Pedido" Command="{Binding FinalizarPedidoCommand}" Width="120"/>
            </StackPanel>

            <TextBlock Text="Itens do Pedido:" FontWeight="Bold" Margin="0,10,0,2"/>
            <ListBox ItemsSource="{Binding ItensPedido}" DisplayMemberPath="NomeProduto" Height="100"/>

            <TextBlock Text="{Binding ValorTotal, StringFormat='Total: {0:C}'}" FontWeight="Bold" FontSize="14" Margin="0,5"/>
        </StackPanel>
    </Grid>
</Window>
